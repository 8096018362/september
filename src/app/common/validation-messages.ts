import { Validators } from '@angular/forms';

export const ValidationMessages = {

    nameOfEmployee: [
        { type: 'required', message: 'Name Of Employee Name is required' }
    ],
    nationalId: [
        { type: 'required', message: 'National ID is required' }
    ],

};
